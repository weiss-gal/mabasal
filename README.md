# mabasal

A catalog app for the Israeli health basket (סל הבריאות) — medicines funded by the government.

## Components

1. **Crawler** — scrapes the official government site and builds a structured JSON catalog
2. **Frontend** — browsable UI with an LLM-powered natural language interface
3. **Backend** (optional) — API layer if needed to serve the catalog data
